## Root environment example for Shopify Bulk Buddy
# Copy to .env and fill with your real Partner App credentials.
# This file is read by both the server (Express) and the Next.js web app.

# Shopify App credentials
SHOPIFY_API_KEY=your_app_key
SHOPIFY_API_SECRET=your_app_secret
NEXT_PUBLIC_SHOPIFY_API_KEY=your_app_key

# Comma separated scopes. Adjust as needed.
SCOPES=read_products,write_products

# Public base URL your app is served from (no trailing slash). For local dev keep http.
APP_URL=http://localhost:3000

# Frontend (Next.js) dev URL
FRONTEND_URL=http://localhost:3001

# API base URL that Next.js should proxy to for /api requests (dev convenience)
API_URL=http://localhost:3000

# 32+ char random string used to encrypt offline tokens at rest.
ENCRYPTION_KEY=replace_with_a_long_random_string
ENCRYPTION_KEY_VERSION=1
# Optional future key for rotation
# ENCRYPTION_KEY_V2=replace_with_next_random_string

# Prisma / SQLite database location (relative to server workspace). Do not change unless needed.
DATABASE_URL=file:../prisma/dev.db

# Ports (optional overrides)
PORT=3000
NEXT_PORT=3001

# Node env
NODE_ENV=development
